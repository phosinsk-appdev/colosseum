<!DOCTYPE html>
<html>
  <head>
    <title>Base Rails</title>
    <%= csrf_meta_tags %>

    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>

  </head>

  <body>

    <table border="1">
      
      <tr>
        <td><a href="/">Home</a></td>
        <td><a href="/events">Events</a></td>
        <td><a href="/players">Players</a></td>
        <td><a href="/games">Games</a></td>
      </tr>

    </table>

    <% if @current_user == nil %>
      <ul style="list-style: none;">
        <li> <a href="/user_sign_in"> Sign In </a> </li>
        <li> <a href="/user_sign_up"> Sign Up </a> </li>
      </ul>
    <% else %>
      <ul style="list-style: none;">
        <li> Welcome, <%=@current_user.email.split('@').first%> <a href="/user_sign_out"> Sign Out </a> </li>
      </ul>
    <% end %>

    <p style="color: green"> <%= notice %> </p>
    <p style="color: red"> <%= alert %> </p>

    <%= yield %>
  </body>
</html>
